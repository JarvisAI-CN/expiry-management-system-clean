Options -Indexes

<IfModule mod_authz_core.c>
    <FilesMatch "\.(sqlite|db)$">
        Require all denied
    </FilesMatch>
</IfModule>

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(app|data)/ - [F,L]
</IfModule>
