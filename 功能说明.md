# 星巴克门店智能效期管理系统 V3.0.0 - 功能说明

## 项目概述

星巴克门店智能效期管理系统是一款专为星巴克门店设计的 Web 效期管理系统，帮助门店员工高效管理库存效期，减少浪费，优化库存管理。

## 安装步骤

### 1. 环境要求
- PHP 8.1+
- MySQL 8.0+
- Apache 或 Nginx 服务器
- 至少 1GB 内存
- 至少 10GB 磁盘空间

### 2. 安装方法

#### 方法一：浏览器安装（推荐）
1. 将项目文件上传到服务器
2. 设置文件权限：
   ```bash
   chmod -R 755 expiry-clean/
   chmod -R 777 expiry-clean/config/
   chmod -R 777 expiry-clean/public/uploads/
   chmod -R 777 expiry-clean/logs/
   ```
3. 在浏览器中访问 `install.php`
4. 按照安装向导完成安装

#### 方法二：命令行安装（开发环境）
```bash
cd expiry-clean
php install.php --cli
```

### 3. 安装完成后
- 系统会创建 `install.lock` 文件
- 访问 `login.php` 登录系统
- 默认账号：admin，密码：admin123

## 功能模块

### 1. 数据导入模块 (admin/import_todo.php)

#### 功能说明
- 用于从 Excel 文件导入产品数据到系统
- 支持 .xls 和 .xlsx 格式
- 数据会先进入待办列表，需要管理员审核和绑定分类

#### 使用步骤

1. **文件上传**
   - 点击「选择文件」按钮或拖拽文件到上传区域
   - 支持文件大小：10MB 以内
   - 支持格式：.xls、.xlsx

2. **数据解析**
   - 系统会自动解析 Excel 文件
   - 验证数据格式和完整性
   - 显示解析结果和错误信息

3. **待办管理**
   - 解析成功的数据会进入待办列表
   - 可以查看、删除、批量处理待办项目
   - 支持按分类筛选待办数据

4. **分类绑定**
   - 选择待办项目
   - 点击「绑定分类」按钮
   - 选择对应分类和盘点频次
   - 系统会将产品与分类关联

#### 支持的列名
```
公司分类 → company_category
商品名称 → product_name
SKU → sku
商品编码 → sku
库存数量 → quantity
数量 → quantity
效期 → expiry_date
到期日期 → expiry_date
```

### 2. 盘点系统 (stocktake.php)

#### 功能说明
- 用于创建和管理盘点单
- 支持扫码枪输入和手动输入
- 提供 AI 智能分析功能
- 支持 Excel 导出

#### 使用步骤

1. **创建盘点单**
   - 点击「创建新盘点单」按钮
   - 系统会生成唯一的盘点编号（如：STK-20260224-0001）
   - 进入盘点界面

2. **添加产品**
   - 使用扫描输入：
     - 使用扫码枪扫描产品条码
     - 系统会自动填充 SKU 字段
   - 手动输入：
     - 输入 SKU、数量和效期
     - 点击「添加」按钮

3. **查看盘点项**
   - 表格显示所有添加的产品
   - 可以删除不需要的产品
   - 显示产品的效期状态（正常/即将过期/已过期）

4. **完成盘点**
   - 点击「完成盘点」按钮
   - 系统会自动生成 AI 智能分析
   - 保存盘点结果

5. **导出数据**
   - 支持导出为 Excel 文件
   - 包含产品信息和效期数据
   - 方便打印和存档

#### 盘点状态
- **草稿**：正在编辑的盘点单
- **已完成**：已完成的盘点单，可以查看详情和导出

## 核心功能

### 1. 效期管理
- 自动计算产品效期状态
- 提前过期提醒
- 过期产品标记

### 2. 分类管理
- 支持多种分类类型
- 可配置提前报废天数
- 可配置提前下架天数
- 可配置盘点频次

### 3. 产品管理
- SKU 唯一标识
- 产品基本信息管理
- 分类关联
- 原始分类记录

### 4. AI 智能分析
- 使用 OpenAI GPT-4 分析盘点数据
- 提供库存优化建议
- 预测过期风险
- 生成详细分析报告

### 5. 数据导出
- 支持 Excel 导出
- 包含完整盘点数据
- 格式标准化，便于打印

## 使用技巧

### 1. 高效盘点
- 使用扫码枪提高效率
- 批量添加产品
- 利用搜索功能快速查找产品

### 2. 数据导入
- 确保 Excel 文件格式正确
- 使用标准列名提高识别率
- 预览数据后再提交

### 3. 分类管理
- 定期检查分类配置
- 根据实际需要调整分类
- 确保分类与产品关联正确

### 4. 系统维护
- 定期备份数据库
- 清理过期数据
- 检查系统日志

## 常见问题

### Q: 无法上传文件？
A: 检查以下几点：
- 上传目录权限是否为 777
- 文件大小是否超过 10MB
- 文件格式是否为 .xls 或 .xlsx

### Q: 导入数据失败？
A: 检查 Excel 文件格式：
- 是否包含必填列（公司分类、商品名称、SKU）
- 日期格式是否正确
- SKU 是否重复

### Q: 产品效期计算错误？
A: 检查系统时间是否正确
- 效期计算基于服务器时间
- 确保服务器时区设置正确

### Q: AI 分析无法生成？
A: 检查以下设置：
- 是否正确配置了 OpenAI API 密钥
- 是否有足够的 API 调用额度
- 网络连接是否正常

## 技术支持

### 系统要求
- PHP 8.1+
- MySQL 8.0+
- 浏览器要求：Chrome 80+, Firefox 75+, Safari 13+

### 服务器要求
- 至少 1GB 内存
- 至少 10GB 磁盘空间
- 支持 HTTPS（推荐）

### 联系方式
如有问题，请联系：
- 邮箱：support@starbucks-expiry.com
- 文档：查看 docs/ 目录下的说明

---

**版本**: V3.0.0  
**更新时间**: 2026-02-24  
**作者**: 资深 PHP 全栈架构师
